import {
  ArgsTable,
  Canvas,
  Description,
  Meta,
  Story,
} from '@storybook/addon-docs'

import VButton, {
  buttonForms,
  buttonSizes,
  buttonVariants,
} from '~/components/VButton.vue'

<Meta
  title="Components/VButton"
  components={VButton}
  argTypes={{
    as: {
      options: buttonForms,
      control: { type: 'radio' },
    },
    variant: {
      options: buttonVariants,
      control: { type: 'select' },
    },
    pressed: { control: 'boolean' },
    size: {
      options: buttonSizes,
      control: { type: 'radio' },
    },
    disabled: { control: 'boolean' },
    focusableWhenDisabled: { control: 'boolean' },
    type: { control: 'text' },
  }}
/>

export const Template = (args) => ({
  template: `
    <VButton v-bind="args" @click="onClick" href="/">
      Code is Poetry
    </VButton>
  `,
  components: { VButton },
  methods: {
    onClick() {
      window.alert('hello!')
    },
  },
  setup() {
    return { args }
  },
})

# VButton

<Description of={VButton} />

<ArgsTable of={VButton} />

<Canvas>
  <Story name="VButton" args={{}}>
    {Template.bind({})}
  </Story>
</Canvas>

## Button variants

export const VariantsTemplate = (args) => ({
  template: `<div class="flex gap-4 flex-wrap">
    <VButton v-bind="args" variant="primary">
      Primary
    </VButton>
    <VButton v-bind="args" variant="secondary">
      Secondary
    </VButton>
    <VButton v-bind="args" variant="tertiary">
      Tertiary
    </VButton>
    <VButton v-bind="args" variant="action-menu">
      Action Menu
    </VButton>
    <VButton v-bind="args" variant="action-menu-secondary">
      Action Menu Secondary
    </VButton>
    <VButton v-bind="args" variant="action-menu-muted">
      Action Menu Muted
    </VButton>
    <VButton v-bind="args" variant="plain">
      Plain
    </VButton>
    <VButton v-bind="args" variant="plain-dangerous">
      Plain Dangerous
    </VButton>
    <VButton v-bind="args" variant="full">
      Full
    </VButton>
  </div>`,
  components: { VButton },
  setup() {
    return { args }
  },
})

<Canvas>
  <Story
    name="VButton Variants"
    args={{}}
    argTypes={{
      pressed: { control: 'boolean' },
      size: {
        options: buttonSizes,
        control: { type: 'radio' },
      },
      disabled: { control: 'boolean' },
    }}
  >
    {VariantsTemplate.bind({})}
  </Story>
</Canvas>
