import { Description, Meta, Canvas, Story } from '@storybook/addon-docs'

import VLanguage from '~/components/VLanguage/VLanguage.vue'

<Meta title="Components/VLanguage" component={VLanguage} />

export const Template = (args) => ({
  template: `
    <VLanguage v-bind="args" />`,
  components: { VLanguage },
  setup() {
    return { args }
  },
})

# VLanguage

<Description of={VLanguage} />

<Canvas>
  <Story name="Default">{Template.bind({})}</Story>
</Canvas>
