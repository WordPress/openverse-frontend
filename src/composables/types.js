/**
 * @template T
 * @typedef {import('#app').Ref<T>} Ref
 */

/**
 * @template T
 * @typedef {import('#app').ToRefs<T>} ToRefs
 */

/**
 * @template T
 * @typedef {import('#app').Ref | T} MaybeRef
 */

export default {}
